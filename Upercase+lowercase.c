int main() {
    char ch = 'a';  

    if (ch >= 'A' && ch <= 'Z') {
        printf("%c is an Uppercase letter", ch);
    } else  {
        printf("%c is a Lowercase letter", ch);
    } 

    return 0;
}
