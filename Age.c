int main() {
    int age = 20; 

    if (age >= 18) {
        printf("Age = %d Eligible to vote", age);
    } else {
        printf("Age = %d NOT eligible to vote", age);
    }
    return 0;
}
